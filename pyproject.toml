[project]
name = "flutterff"
version = "2.2.0"
description = "Lightweight native Flutter web development launcher for Linux"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pywebview>=6.1",
]
